Escribe una función JavaScript que acepte dos argumentos: un identificador de elemento HTML, y una cadena de texto HTML.
La función, cuando sea ejecutada, buscará ese elemento en la página HTML que está cargada en el navegador, y cambiará su contenido HTML por el que indique el segundo argumento.
